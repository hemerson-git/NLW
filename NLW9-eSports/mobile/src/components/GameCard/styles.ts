import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  cover: {
    width: 240,
    height: 320,
    justifyContent: "flex-end",
    borderRadius: 8,
    overflow: "hidden",
  },

  footer: {
    width: "100%",
    height: 120,
    padding: 16,
    justifyContent: "flex-end",
  },
});
