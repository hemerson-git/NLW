import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  listContainer: {
    paddingRight: 72,
  },

  emptyListContainer: {
    alignItems: "center",
    textAlign: "center",
    flex: 1,
    marginRight: 40,
  },
});
